{
  "name": "local-node-lt-mre",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clear": "rm -rf build",
    "build": "mkdir -p build && everdev sol compile contracts/* --output-dir build",
    "test": "ts-node tests/1-emit.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "everscale-inpage-provider": "^0.3.52",
    "everscale-standalone-client": "^2.1.16"
  }
}
